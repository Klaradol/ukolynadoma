const cards = document.querySelectorAll('.card');
const flipCardButton = document.getElementById('flip-card-button');
const checkPairsButton = document.getElementById('check-pairs-button');
 
let flippedCards = [];
let pairsFound = 0;

const button = document.querySelector("button"); button.addEventListener("click", myFunction);
function myFunction() {   alert("Máte všechny karty správně!"); }

